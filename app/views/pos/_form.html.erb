<%= form_for @po do |f| %>
  <% if @po.errors.any? %>
    <div class="errors">
      <h2><%= pluralize(@po.errors.count, "error")%> prohibited this entry from being saved:</h2>
        <ul>
          <% @po.errors.full_messages.each do |d| %>
		    <li><%= msg %></li>
		  <% end %>
        </ul>
    </div>
  <% end %>
  
  <div class="field">
  	<%= f.label :po_date %>
	<%= f.text_field :po_date, :class => 'calendar' %>
  </div>
  
  <div class="field">
  	<%= f.label :total_amount %>
	<%= f.text_field :total_amount %>
  </div>
  
  <div class="field">
  	<%= f.label :notes %>
	<%= f.text_area :note %>
  </div>
  
  <div class="actions">
  	<%= f.submit 'Save' %>&nbsp;&nbsp;&nbsp;<%= link_to 'Cancel', pos_path %>
  </div>
<% end %>
