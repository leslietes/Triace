<%= form_for @order do |f| %>
  <% if @order.errors.any? %>
    <div class="errors">
      <h2><%= pluralize(@order.errors.count, "error")%> prohibited this entry from being saved:</h2>
        <ul>
          <% @order.errors.full_messages.each do |d| %>
		    <li><%= msg %></li>
		  <% end %>
        </ul>
    </div>
  <% end %>
  
  <div class="field">
  	<%= f.label :customer %>
	<%= f.select :customer_id, options_from_collection_for_select(@customers, "id","name",@order.customer_id), :include_blank => true, :class => "focus" %>
  </div>
  
  <div class="field">
  	<%= f.label :order_date %>
	<%= f.text_field :order_date, :class => 'calendar' %>
  </div>
  
  <div class="field">
  	<%= f.label :total_amount %>
	<%= f.text_field :total_amount %>
  </div>
  
  <%# f.text_field :terms %>
  
  <div class="actions">
  	<%= f.submit 'Save' %>&nbsp;&nbsp;&nbsp;<%= link_to 'Cancel', orders_path %>
  </div>
<% end %>
